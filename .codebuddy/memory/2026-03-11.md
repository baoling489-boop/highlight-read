# 2026-03-11 工作日志

## 修复 epubjs themes.override 调用方式
- **问题**：字号、行间距、页边距调节无效
- **根因**：`themes.override('font-size', '18px !important')` 中 `!important` 被 `setProperty` 当成值的一部分，导致 CSS 值无效。`setProperty` 需要通过第三个参数 `"important"` 来设置优先级
- **修复**：将所有 `themes.override` 调用中的 `!important` 从值中移除，改为通过第三个参数 `priority=true` 传递
- **影响范围**：font-size、line-height、padding、color、background 五处 override 调用
- **文件**：`src/components/EpubReader.tsx` 第 527-555 行

## GitHub Pages 部署准备
- 修改 `vite.config.ts`，添加 `base: '/highlight-read/'` 配置
- 创建 `.github/workflows/deploy.yml` GitHub Actions 自动部署工作流
- 创建 `.gitignore`，排除 `node_modules/`、`dist/` 等文件
- Git 仓库已初始化，文件已暂存，等待用户注册 GitHub 并创建仓库后推送

## GitHub Pages 部署完成
- 仓库：`https://github.com/baoling489-boop/highlight-read`
- 部署方式：gh-pages 分支（Deploy from a branch）
- 使用 CodeBuddy 内置终端执行 `npm run build`，生成带正确 base 路径 `/highlight-read/` 的产物
- 将 dist 目录手动推送到 gh-pages 分支（`git push -f origin gh-pages`）
- 页面地址：`https://baoling489-boop.github.io/highlight-read/`

## 创建 README.md 项目文档
- 创建 `README.md`，包含完整的功能介绍、界面预览、本地运行指南、技术栈、项目结构和使用指南
- 涵盖 EPUB 阅读、多彩高亮、ADHD 专注模式、阅读时间统计、可读性自定义等全部功能说明
