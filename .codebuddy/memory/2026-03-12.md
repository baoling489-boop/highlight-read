# 2026-03-12 工作记录

## ADHD 阅读增强功能改进

### 1. 句首加粗增加 font-size 渐变
- 在 `src/nlp/highlighter.ts` 的 `generateADHDStyles()` 中，给 `.adhd-head-1~5` 增加了 font-size 渐变：
  - head-1: 1.08em, head-2: 1.06em, head-3: 1.04em, head-4: 1.02em, head-5: 1.00em
  - 暗色模式和浅色模式均已添加
  - 形成三维渐变：字号 + 字重 + 颜色

### 2. 阅读聚焦（鼠标跟随 3 行高亮）
- 在 `src/components/EpubReader.tsx` 中实现 `setupRegionFocus`
- 模式：鼠标跟随 + caretRangeFromPoint 行吸附 + 3 行聚焦窗口
- 视觉样式：蓝紫色背景 + 蓝色左边框 + 6px 圆角 + 上下微暗遮罩
- CSS 在 `highlighter.ts` 中统一管理（暗色/浅色模式自适应，不再区分 dark/light 两套）
- 曾尝试复杂的段落合并智能区域聚焦方案，体验不好，回退到简洁的 overlay 3行模式

### 3. Toolbar 文案更新
- 在 `src/components/Toolbar.tsx` 中：
  - "聚焦行高亮" → "阅读聚焦"
  - 描述："鼠标所在行高亮聚焦，上下区域微暗"
  - 句首强化描述增加 "+微放大"
