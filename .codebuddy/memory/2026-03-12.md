# 2026-03-12 工作日志

## 电子书阅读器阅读体验优化（不新增功能，纯体验提升）

### 修改文件
- `src/components/EpubReader.tsx` — injectCustomStyles 函数大幅扩充、键盘滚动逻辑重写、热区视觉反馈
- `src/components/Toolbar.tsx` — 工具栏自动隐藏（3秒无操作收起，鼠标到顶部滑出）
- `src/styles/global.css` — 热区 hover CSS 动画

### 优化清单（15项）
1. **最大阅读宽度** — body max-width: 42em，每行控制 35-42 字符
2. **标题字号层次** — h1~h4 递减字号（1.6em/1.4em/1.2em/1.1em），line-height 收紧至 1.4
3. **段间距联动** — margin-bottom 改为 0.6lh，与字号/行高联动
4. **响应式内边距** — padding: 20px clamp(16px, 5vw, 48px)
5. **中英文混排** — word-break: break-word; overflow-wrap: break-word
6. **字体回退策略** — 增加 'Songti SC'、'STSong'、'SimSun' 等系统内置中文字体
7. **颜色对比度微调** — 浅色 #2c2c2c（降低纯黑刺眼），暗色 #d4d4d4（降低亮白）
8. **主题切换过渡动画** — body transition: background-color/color 0.3s ease
9. **暗色模式元素适配** — a/blockquote/code/pre/hr/table 都有暗色样式
10. **iframe 滚动条细化** — 6px 细滚动条 + 圆角 + 半透明
11. **iframe smooth scroll** — html scroll-behavior: smooth
12. **键盘平滑滚动** — ArrowDown/Space/PageDown 向下滚 85% 视口，到底翻章；ArrowUp/PageUp 同理
13. **热区视觉反馈** — hover 时渐变背景 + 箭头图标淡入
14. **工具栏自动隐藏** — 3秒无活动收起，鼠标移到顶部 60px 区域滑出
15. **图片圆角** — img border-radius: 4px
